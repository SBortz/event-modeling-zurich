---
import "../styles/global.css";
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Event Modeling - Visualizing Software Systems</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
    
    /* Aktive Navigation für Hash-Links */
    nav a[href^="#"].active {
      color: white !important;
      background-color: rgb(37 99 235) !important; /* blue-600 */
    }
    
    /* Standard-Zustand für Hash-Links */
    nav a[href^="#"] {
      color: rgb(75 85 99); /* gray-600 */
    }
    
    /* Home-Navigation-Link (nicht der Logo-Link) */
    nav .desktop-nav a[href="/"].active {
      color: white !important;
      background-color: rgb(37 99 235) !important; /* blue-600 */
    }
    
    /* Logo-Link immer im Standard-Zustand */
    nav .logo-link {
      color: rgb(15 31 47) !important; /* #0f1f2f */
      background-color: transparent !important;
    }
  </style>
</head>
<body class="bg-gradient-to-b from-[#eaf4fb] to-white text-[#1f1f1f] leading-relaxed">
  <!-- Navigation -->
  <nav class="bg-white/90 backdrop-blur-sm sticky top-0 z-50 border-b border-gray-200">
    <div class="max-w-6xl mx-auto px-4">
      <div class="flex items-center justify-between h-16">
        <!-- Logo/Brand -->
        <a href="/" class="logo-link flex items-center space-x-2">
          <span class="text-2xl">🗓️</span>
          <span class="font-bold text-xl text-[#0f1f2f]">Event Modeling Zurich</span>
        </a>
        
        <!-- Desktop Navigation -->
        <div class="desktop-nav hidden md:flex items-center space-x-8">
          <a href="/" class="text-gray-600 hover:text-blue-600 px-4 py-2 rounded-lg transition-colors duration-200">Home</a>
          <a href="/meetup" class="text-gray-600 hover:text-blue-600 px-4 py-2 rounded-lg transition-colors duration-200">Meetup</a>
          <a href="/blog" class="text-gray-600 hover:text-blue-600 px-4 py-2 rounded-lg transition-colors duration-200">Blog</a>
          <a href="#about" class="text-gray-600 hover:text-blue-600 px-4 py-2 rounded-lg transition-colors duration-200">About</a>
          <a href="#contact" class="text-gray-600 hover:text-blue-600 px-4 py-2 rounded-lg transition-colors duration-200">Contact</a>
        </div>
        
        <!-- CTA Button -->
        <div class="hidden md:block">
          <a href="/meetup" class="bg-yellow-400 hover:bg-yellow-500 text-white font-semibold px-4 py-2 rounded-lg transition-colors duration-200">
            Join Meetup
          </a>
        </div>
        
        <!-- Mobile menu button -->
        <button class="md:hidden p-2 rounded-md text-gray-600 hover:text-gray-900 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-blue-500">
          <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
      
      <!-- Mobile Navigation -->
      <div class="md:hidden hidden" id="mobile-menu">
        <div class="px-2 pt-2 pb-3 space-y-1 border-t border-gray-200">
          <a href="/" class="block px-3 py-2 text-white font-medium bg-blue-600 rounded-md">Home</a>
          <a href="/meetup" class="block px-3 py-2 text-gray-600 hover:text-blue-600 hover:bg-gray-50 rounded-md">Meetup</a>
          <a href="/blog" class="block px-3 py-2 text-gray-600 hover:text-blue-600 hover:bg-gray-50 rounded-md">Blog</a>
          <a href="#about" class="block px-3 py-2 text-gray-600 hover:text-blue-600 hover:bg-gray-50 rounded-md">About</a>
          <a href="#contact" class="block px-3 py-2 text-gray-600 hover:text-blue-600 hover:bg-gray-50 rounded-md">Contact</a>
          <div class="pt-4">
            <a href="/meetup" class="block w-full text-center bg-yellow-400 hover:bg-yellow-500 text-white font-semibold px-4 py-2 rounded-lg transition-colors duration-200">
              Join Meetup
            </a>
          </div>
        </div>
      </div>
    </div>
  </nav>

  <div class="max-w-6xl mx-auto">
    <!-- Hero Section -->
    <header class="relative text-left pt-5">
      <div class="absolute top-10 left-0 z-10 px-6 py-4 flex">
        <p class="sm:text-4xl lg:text-6xl mr-4">🗓️</p>
        <h1 class="sm:text-4xl lg:text-6xl font-bold text-[#0f1f2f] leading-tight">
           Event Modeling <br class="hidden md:block" />Zurich
        </h1>
      </div>
      <img src="zurich-skyline-placeholder9.png" alt="Zurich Skyline" class="w-full h-auto block rounded-t-3xl" loading="lazy" />
    </header>

    <!-- What is Event Modeling Section -->
    <section class="px-4 py-20 bg-white">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-4xl font-bold mb-8 text-[#0f1f2f]">What is Event Modeling?</h2>
        <p class="text-xl mb-8 leading-relaxed">
          Event Modeling is a <strong>visual method for describing software systems as stories</strong>. 
          It helps teams align by mapping out what happens, when, and why – across UI, logic, and data.
        </p>
        <p class="text-xl mb-12 leading-relaxed">
          Instead of starting with data structures or APIs, Event Modeling begins with the <strong>events that matter to your business</strong> 
          and builds understanding from there.
        </p>
        
        <div class="flex flex-col items-center p-8 rounded-lg bg-gray-50">
          <img
            src="methodik.png"
            alt="Event Model Timeline Diagram"
            class="w-full max-w-4xl rounded-lg mb-6 shadow-lg"
            loading="lazy"
          />
          <p class="text-lg text-gray-700">A system story told in events – from trigger to state</p>
        </div>
      </div>
    </section>

    <!-- Key Benefits Section -->
    <section class="px-4 py-20 bg-gray-50">
      <div class="max-w-5xl mx-auto">
        <h2 class="text-3xl font-bold mb-12 text-center text-[#0f1f2f]">Why Event Modeling?</h2>
        <div class="grid md:grid-cols-3 gap-8">
          <div class="bg-white p-6 rounded-lg shadow-sm">
            <div class="text-3xl mb-4">🎯</div>
            <h3 class="text-xl font-semibold mb-3">Business Alignment</h3>
            <p class="text-gray-700">Bridge the gap between business stakeholders and technical teams with clear, visual communication.</p>
          </div>
          <div class="bg-white p-6 rounded-lg shadow-sm">
            <div class="text-3xl mb-4">🧠</div>
            <h3 class="text-xl font-semibold mb-3">Complexity Management</h3>
            <p class="text-gray-700">Break down complex systems into understandable, manageable pieces through event-driven thinking.</p>
          </div>
          <div class="bg-white p-6 rounded-lg shadow-sm">
            <div class="text-3xl mb-4">🚀</div>
            <h3 class="text-xl font-semibold mb-3">Scalable Architecture</h3>
            <p class="text-gray-700">Design systems that can evolve and scale while maintaining clarity and consistency.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Core Concepts Section -->
    <section class="px-4 py-20 bg-white">
      <div class="max-w-5xl mx-auto">
        <h2 class="text-3xl font-bold mb-12 text-center text-[#0f1f2f]">Core Concepts</h2>
        <div class="grid md:grid-cols-2 gap-12">
          <div>
            <h3 class="text-2xl font-semibold mb-4 text-[#0f1f2f]">Events</h3>
            <p class="text-lg mb-4 text-gray-700">
              Events are the things that happen in your system that matter to your business. 
              They represent facts that have occurred and cannot be changed.
            </p>
            <ul class="list-disc list-inside text-gray-700 space-y-2">
              <li>User registered</li>
              <li>Order placed</li>
              <li>Payment processed</li>
              <li>Shipment delivered</li>
            </ul>
          </div>
          <div>
            <h3 class="text-2xl font-semibold mb-4 text-[#0f1f2f]">Commands</h3>
            <p class="text-lg mb-4 text-gray-700">
              Commands represent intentions to change the system state. 
              They are validated and can either succeed or fail.
            </p>
            <ul class="list-disc list-inside text-gray-700 space-y-2">
              <li>Register user</li>
              <li>Place order</li>
              <li>Process payment</li>
              <li>Schedule shipment</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- About Section -->
    <section id="about" class="px-4 py-20 bg-gray-50">
      <div class="max-w-5xl mx-auto">
        <h2 class="text-3xl font-bold mb-12 text-center text-[#0f1f2f]">About Event Modeling Zurich</h2>
        <div class="grid md:grid-cols-2 gap-12 items-center">
          <div>
            <h3 class="text-2xl font-semibold mb-6 text-[#0f1f2f]">Our Mission</h3>
            <p class="text-lg mb-6 text-gray-700 leading-relaxed">
              We're passionate about bringing clarity to complex software systems through Event Modeling. 
              Our community in Zurich brings together professionals who believe that understanding is the foundation of great software.
            </p>
            <p class="text-lg mb-6 text-gray-700 leading-relaxed">
              Whether you're a seasoned architect or just starting your journey with Event Modeling, 
              you'll find a welcoming environment to learn, share, and grow.
            </p>
            <div class="flex items-center space-x-4">
              <div class="text-2xl">🇨🇭</div>
              <div>
                <p class="font-semibold text-[#0f1f2f]">Based in Zurich</p>
                <p class="text-gray-600">Switzerland's tech hub</p>
              </div>
            </div>
          </div>
          <div class="bg-white p-8 rounded-lg shadow-sm">
            <h3 class="text-xl font-semibold mb-4 text-[#0f1f2f]">What We Do</h3>
            <ul class="space-y-3 text-gray-700">
              <li class="flex items-center">
                <span class="text-green-500 mr-3">✓</span>
                Monthly meetups and workshops
              </li>
              <li class="flex items-center">
                <span class="text-green-500 mr-3">✓</span>
                Hands-on Event Modeling sessions
              </li>
              <li class="flex items-center">
                <span class="text-green-500 mr-3">✓</span>
                Networking with industry experts
              </li>
              <li class="flex items-center">
                <span class="text-green-500 mr-3">✓</span>
                Knowledge sharing and collaboration
              </li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- Zurich Meetup Section -->
    <section class="px-4 py-20 bg-gradient-to-r from-blue-50 to-indigo-50">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-3xl font-bold mb-6 text-[#0f1f2f]">Join Our Zurich Meetup</h2>
        <p class="text-xl mb-8 leading-relaxed">
          Connect with fellow system thinkers, architects, developers & modelers in Zurich. 
          Together, we'll explore how complex software systems can become understandable and manageable.
        </p>
        <div class="bg-white p-8 rounded-lg shadow-sm mb-8">
          <h3 class="text-2xl font-semibold mb-4">Next Meetup</h3>
          <p class="text-lg mb-6">Stay tuned – more info coming soon!</p>
          <a href="/meetup" class="inline-block bg-yellow-400 hover:bg-yellow-500 text-white font-semibold text-lg px-8 py-4 rounded-lg shadow transition-colors duration-200">
            Learn More About Our Meetups
          </a>
        </div>
      </div>
    </section>

    <!-- Blog Section -->
    <section class="px-4 py-20 bg-white">
      <div class="max-w-5xl mx-auto">
        <h2 class="text-3xl font-bold mb-12 text-center text-[#0f1f2f]">Latest Insights</h2>
        <div class="grid md:grid-cols-2 gap-8">
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-xl font-semibold mb-3 text-[#0f1f2f]">Getting Started with Event Modeling</h3>
            <p class="text-gray-700 mb-4">Learn the basics of Event Modeling and how to apply it to your first project.</p>
            <a href="#" class="text-blue-600 hover:text-blue-800 font-medium">Read more →</a>
          </div>
          <div class="bg-gray-50 p-6 rounded-lg">
            <h3 class="text-xl font-semibold mb-3 text-[#0f1f2f]">Event Sourcing vs Event Modeling</h3>
            <p class="text-gray-700 mb-4">Understanding the differences and when to use each approach.</p>
            <a href="#" class="text-blue-600 hover:text-blue-800 font-medium">Read more →</a>
          </div>
        </div>
        <div class="text-center mt-8">
          <a href="/blog" class="inline-block border-2 border-[#1f1f1f] text-[#1f1f1f] px-6 py-3 rounded-md font-semibold bg-white hover:bg-gray-100 transition-colors duration-200">
            View All Articles
          </a>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="px-4 py-20 bg-gray-100 text-center rounded-b-xl mb-20">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-3xl font-bold mb-6 text-[#0f1f2f]">Get in Touch</h2>
        <p class="text-xl mb-8 text-gray-700">Connect with our community and stay updated on the latest Event Modeling insights</p>
        
        <div class="grid md:grid-cols-3 gap-8 mb-12">
          <div class="bg-white p-6 rounded-lg shadow-sm">
            <div class="text-3xl mb-4">📧</div>
            <h3 class="text-lg font-semibold mb-2 text-[#0f1f2f]">Newsletter</h3>
            <p class="text-gray-600 mb-4">Get monthly updates and insights</p>
            <div class="flex flex-col gap-3">
              <input 
                type="email" 
                placeholder="Your email" 
                class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
              >
              <button class="w-full bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md font-medium transition-colors duration-200">
                Subscribe
              </button>
            </div>
          </div>
          
          <div class="bg-white p-6 rounded-lg shadow-sm">
            <div class="text-3xl mb-4">🔗</div>
            <h3 class="text-lg font-semibold mb-2 text-[#0f1f2f]">LinkedIn</h3>
            <p class="text-gray-600 mb-4">Follow our professional updates</p>
            <a href="https://www.linkedin.com/in/sebastian-bortz-95273888" class="inline-block bg-[#0A66C2] text-white px-4 py-2 rounded-md font-medium hover:bg-[#004182] transition-colors duration-200">
              Follow Us
            </a>
          </div>
          
          <div class="bg-white p-6 rounded-lg shadow-sm">
            <div class="text-3xl mb-4">🤝</div>
            <h3 class="text-lg font-semibold mb-2 text-[#0f1f2f]">Meetup</h3>
            <p class="text-gray-600 mb-4">Join our local community</p>
            <a href="https://www.meetup.com/event-modelling-zurich/" class="inline-block border-2 border-[#1f1f1f] text-[#1f1f1f] px-4 py-2 rounded-md font-medium bg-white hover:bg-gray-100 transition-colors duration-200">
              Join Group
            </a>
          </div>
        </div>
        
        <div class="bg-white p-8 rounded-lg shadow-sm">
          <h3 class="text-2xl font-semibold mb-4 text-[#0f1f2f]">Have Questions?</h3>
          <p class="text-lg mb-6 text-gray-700">We'd love to hear from you! Whether you have questions about Event Modeling or want to get involved in our community.</p>
          <div class="flex flex-col sm:flex-row justify-center items-center gap-4">
            <a href="mailto:info@eventmodeling-zurich.ch" class="bg-yellow-400 hover:bg-yellow-500 text-white px-6 py-3 rounded-lg font-semibold transition-colors duration-200">
              Send us an Email
            </a>
            <a href="/meetup" class="border-2 border-[#1f1f1f] text-[#1f1f1f] px-6 py-3 rounded-lg font-semibold bg-white hover:bg-gray-100 transition-colors duration-200">
              Learn More
            </a>
          </div>
        </div>
      </div>
    </section>
  </div>

  <!-- Additional spacing for better scroll detection -->
  <div class="h-96 bg-transparent"></div>

  <!-- JavaScript for mobile menu and hash-based navigation -->
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const mobileMenuButton = document.querySelector('button[class*="md:hidden"]');
      const mobileMenu = document.getElementById('mobile-menu');
      
      if (mobileMenuButton && mobileMenu) {
        mobileMenuButton.addEventListener('click', function() {
          mobileMenu.classList.toggle('hidden');
        });
        
        // Close mobile menu when clicking on a link
        const mobileMenuLinks = mobileMenu.querySelectorAll('a');
        mobileMenuLinks.forEach(link => {
          link.addEventListener('click', function() {
            mobileMenu.classList.add('hidden');
          });
        });
      }

      // Aktive Navigation basierend auf Scroll-Position
      function updateActiveNavigation() {
        const sections = document.querySelectorAll('section[id]');
        const navLinks = document.querySelectorAll('nav a[href^="#"]');
        const homeNavLink = document.querySelector('nav .desktop-nav a[href="/"]');
        const scrollY = window.pageYOffset;
        const windowHeight = window.innerHeight;
        
        console.log(`ScrollY: ${scrollY}, Window Height: ${windowHeight}`);
        console.log(`Found ${sections.length} sections with IDs`);
        
        let currentSection = '';
        
        sections.forEach(section => {
          const sectionTop = (section as HTMLElement).offsetTop;
          const sectionHeight = (section as HTMLElement).clientHeight;
          const sectionId = section.getAttribute('id');
          
          // Mehrere Kriterien für "sichtbar"
          const isInViewport = scrollY + 200 >= sectionTop && scrollY + 200 < sectionTop + sectionHeight;
          const isNearTop = scrollY >= sectionTop - 200 && scrollY < sectionTop + 200;
          
          console.log(`Section ${sectionId}: top=${sectionTop}, height=${sectionHeight}, scrollY=${scrollY}, inViewport=${isInViewport}, nearTop=${isNearTop}`);
          
          if (isInViewport || isNearTop) {
            if (sectionId) {
              currentSection = sectionId;
              console.log(`Current section set to: ${currentSection}`);
            }
          }
        });
        
        // Alle Hash-Links zurücksetzen
        navLinks.forEach(link => {
          link.classList.remove('active');
        });
        
        // Home-Navigation-Link zurücksetzen (immer inaktiv wenn Hash-Links aktiv sind)
        if (homeNavLink) {
          homeNavLink.classList.remove('active');
          // Ursprüngliche CSS-Klassen wiederherstellen
          homeNavLink.classList.remove('text-white', 'bg-blue-600');
          homeNavLink.classList.add('text-gray-600');
        }
        
        // Aktiven Hash-Link markieren
        if (currentSection) {
          const activeLink = document.querySelector(`nav a[href="#${currentSection}"]`);
          if (activeLink) {
            activeLink.classList.add('active');
            console.log(`Activated link: #${currentSection}`);
          }
        } else {
          // Wenn keine Hash-Sektion aktiv ist, Home-Navigation-Link aktiv machen
          if (homeNavLink) {
            homeNavLink.classList.add('active');
            // Aktive CSS-Klassen für Home setzen
            homeNavLink.classList.remove('text-gray-600');
            homeNavLink.classList.add('text-white', 'bg-blue-600');
            console.log('Activated Home navigation link');
          }
        }
      }

      // Navigation beim Scrollen aktualisieren
      window.addEventListener('scroll', updateActiveNavigation);
      
      // Initial Navigation setzen
      updateActiveNavigation();
    });
  </script>
</body>
</html>
